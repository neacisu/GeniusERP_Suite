# Numeriqo Application Configuration
# Copy this file to .numeriqo.env and fill in the values
# NOTE: This file contains NON-SECRET configuration only
# SECRETS (DB passwords, JWT secrets, API keys) are injected via OpenBao Agent

# ============================================================================
# Container Configuration
# ============================================================================
NUMQ_CONTAINER_NAME=genius-suite-numeriqo-app
NUMQ_RESTART_POLICY=unless-stopped

# ============================================================================
# Application Ports
# ============================================================================
NUMQ_APP_PORT=6750
NUMQ_APP_METRICS_PORT=9090
NUMQ_APP_WORKER_PORT=6751

# ============================================================================
# Application Environment
# ============================================================================
NUMQ_APP_NODE_ENV=production
NUMQ_LOG_LEVEL=info
NUMQ_OTEL_SERVICE_NAME=numeriqo.app

# ============================================================================
# OpenBao Agent Configuration
# ============================================================================
NUMQ_BAO_AGENT_CONFIG=/openbao/agent-config.hcl
NUMQ_APPROLE_ROLE_ID_PATH=../../.secrets/approle/numeriqo/role-id
NUMQ_APPROLE_SECRET_ID_PATH=../../.secrets/approle/numeriqo/secret-id

# ============================================================================
# Traefik Configuration
# ============================================================================
NUMQ_TRAEFIK_ENABLE=true
NUMQ_TRAEFIK_HOST=numeriqo.${SUITE_APP_DOMAIN:-geniuserp.app}
NUMQ_TRAEFIK_ENTRYPOINT=websecure
NUMQ_TRAEFIK_TLS=true
NUMQ_TRAEFIK_CERTRESOLVER=letsencrypt
NUMQ_TRAEFIK_MIDDLEWARES=global-chain@file

# ============================================================================
# Health Check Configuration
# ============================================================================
NUMQ_HEALTHCHECK_INTERVAL=30s
NUMQ_HEALTHCHECK_TIMEOUT=10s
NUMQ_HEALTHCHECK_RETRIES=3
NUMQ_HEALTHCHECK_START_PERIOD=60s

# ============================================================================
# SECRETS - DO NOT DEFINE HERE!
# ============================================================================
# The following secrets are injected via OpenBao Agent:
# - NUMQ_DB_USER (dynamic from database/creds/numeriqo_runtime)
# - NUMQ_DB_PASS (dynamic from database/creds/numeriqo_runtime)
# - NUMQ_JWT_SECRET (static from kv/data/apps/numeriqo)
# - NUMQ_API_KEY (static from kv/data/apps/numeriqo)
# - NUMQ_ENCRYPTION_KEY (static from kv/data/apps/numeriqo)
#
# These are rendered by OpenBao Agent into /app/secrets/.env
# DO NOT add them to this file!
