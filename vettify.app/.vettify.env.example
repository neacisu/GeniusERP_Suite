# Vettify Application Configuration
# Copy this file to .vettify.env and fill in the values
# NOTE: This file contains NON-SECRET configuration only
# SECRETS (DB passwords, JWT secrets, API keys) are injected via OpenBao Agent

# ============================================================================
# Container Configuration
# ============================================================================
VETFY_CONTAINER_NAME=genius-suite-vettify-app
VETFY_RESTART_POLICY=unless-stopped

# ============================================================================
# Application Ports
# ============================================================================
VETFY_APP_PORT=6900
VETFY_APP_METRICS_PORT=6990
VETFY_APP_WORKER_PORT=6901

# ============================================================================
# Application Environment
# ============================================================================
VETFY_APP_NODE_ENV=production
VETFY_LOG_LEVEL=info
VETFY_OTEL_SERVICE_NAME=vettify.app

# ============================================================================
# OpenBao Agent Configuration
# ============================================================================
VETFY_BAO_AGENT_CONFIG=/openbao/agent-config.hcl
VETFY_APPROLE_ROLE_ID_PATH=../../.secrets/approle/vettify/role-id
VETFY_APPROLE_SECRET_ID_PATH=../../.secrets/approle/vettify/secret-id

# ============================================================================
# Traefik Configuration
# ============================================================================
VETFY_TRAEFIK_ENABLE=true
VETFY_TRAEFIK_HOST=vettify.${SUITE_APP_DOMAIN:-geniuserp.app}
VETFY_TRAEFIK_ENTRYPOINT=websecure
VETFY_TRAEFIK_TLS=true
VETFY_TRAEFIK_CERTRESOLVER=letsencrypt
VETFY_TRAEFIK_MIDDLEWARES=global-chain@file

# ============================================================================
# Health Check Configuration
# ============================================================================
VETFY_HEALTHCHECK_INTERVAL=30s
VETFY_HEALTHCHECK_TIMEOUT=10s
VETFY_HEALTHCHECK_RETRIES=3
VETFY_HEALTHCHECK_START_PERIOD=60s

# ============================================================================
# SECRETS - DO NOT DEFINE HERE!
# ============================================================================
# The following secrets are injected via OpenBao Agent:
# - VETFY_DB_USER (dynamic from database/creds/vettify_runtime)
# - VETFY_DB_PASS (dynamic from database/creds/vettify_runtime)
# - VETFY_JWT_SECRET (static from kv/data/apps/vettify)
# - VETFY_API_KEY (static from kv/data/apps/vettify)
# - VETFY_ENCRYPTION_KEY (static from kv/data/apps/vettify)
#
# These are rendered by OpenBao Agent into /app/secrets/.env
# DO NOT add them to this file!
