version: '3.9'

services:
  archify-app:
    build:
      context: ../..
      dockerfile: archify.app/Dockerfile
    container_name: archify-app
    extra_hosts:
      - "postgres:10.10.0.3"
    labels:
      - traefik.enable=true
      - traefik.docker.network=traefik_default
      - traefik.http.routers.archify-app.rule=Host(`archify.geniuserp.app`)
      - traefik.http.routers.archify-app.entrypoints=websecure
      - traefik.http.routers.archify-app.tls.certresolver=cloudflare
      - traefik.http.routers.archify-app.middlewares=security-headers@file
      - traefik.http.services.archify-app.loadbalancer.server.port=6500
    restart: unless-stopped
    env_file:
      - ../../.suite.general.env
      - ../.archify.env
    environment:
      NODE_ENV: ${ARCHY_APP_NODE_ENV:-production}
      PORT: ${ARCHY_APP_PORT:-6500}
      OTEL_EXPORTER_OTLP_ENDPOINT: "${SUITE_OBS_OTEL_COLLECTOR_HTTP_URL:-http://otel-collector:4318}/v1/traces"
      OTEL_SERVICE_NAME: archify.app
    networks:
      - traefik_default
      - observability
      - backing
      - data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:${ARCHY_APP_PORT:-6500}/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s


networks:
  traefik_default:
    name: traefik_default
    external: true
  observability:
    name: observability
    external: true
  backing:
    name: backing
    external: true
  data:
    name: data
    external: true
