# Archify Application Configuration
# Copy this file to .archify.env and fill in the values
# NOTE: This file contains NON-SECRET configuration only
# SECRETS (DB passwords, JWT secrets, API keys) are injected via OpenBao Agent

# ============================================================================
# Container Configuration
# ============================================================================
ARCHY_CONTAINER_NAME=genius-suite-archify-app
ARCHY_RESTART_POLICY=unless-stopped

# ============================================================================
# Application Ports
# ============================================================================
ARCHY_APP_PORT=6500
ARCHY_APP_METRICS_PORT=9091
ARCHY_APP_WORKER_PORT=6501

# ============================================================================
# Application Environment
# ============================================================================
ARCHY_APP_NODE_ENV=production
ARCHY_LOG_LEVEL=info
ARCHY_OTEL_SERVICE_NAME=archify.app

# ============================================================================
# OpenBao Agent Configuration
# ============================================================================
ARCHY_BAO_AGENT_CONFIG=/openbao/agent-config.hcl
ARCHY_APPROLE_ROLE_ID_PATH=../../.secrets/approle/archify/role-id
ARCHY_APPROLE_SECRET_ID_PATH=../../.secrets/approle/archify/secret-id

# ============================================================================
# Traefik Configuration
# ============================================================================
ARCHY_TRAEFIK_ENABLE=true
ARCHY_TRAEFIK_HOST=archify.${SUITE_APP_DOMAIN:-geniuserp.app}
ARCHY_TRAEFIK_ENTRYPOINT=websecure
ARCHY_TRAEFIK_TLS=true
ARCHY_TRAEFIK_CERTRESOLVER=letsencrypt
ARCHY_TRAEFIK_MIDDLEWARES=global-chain@file

# ============================================================================
# Health Check Configuration
# ============================================================================
ARCHY_HEALTHCHECK_INTERVAL=30s
ARCHY_HEALTHCHECK_TIMEOUT=10s
ARCHY_HEALTHCHECK_RETRIES=3
ARCHY_HEALTHCHECK_START_PERIOD=60s

# ============================================================================
# SECRETS - DO NOT DEFINE HERE!
# ============================================================================
# The following secrets are injected via OpenBao Agent:
# - ARCHY_DB_USER (dynamic from database/creds/archify_runtime)
# - ARCHY_DB_PASS (dynamic from database/creds/archify_runtime)
# - ARCHY_JWT_SECRET (static from kv/data/apps/archify)
# - ARCHY_API_KEY (static from kv/data/apps/archify)
# - ARCHY_ENCRYPTION_KEY (static from kv/data/apps/archify)
#
# These are rendered by OpenBao Agent into /app/secrets/.env
# DO NOT add them to this file!
