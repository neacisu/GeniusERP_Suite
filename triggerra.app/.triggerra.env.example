# Triggerra Application Configuration
# Copy this file to .triggerra.env and fill in the values
# NOTE: This file contains NON-SECRET configuration only
# SECRETS (DB passwords, JWT secrets, API keys) are injected via OpenBao Agent

# ============================================================================
# Container Configuration
# ============================================================================
TRIGR_CONTAINER_NAME=genius-suite-triggerra-app
TRIGR_RESTART_POLICY=unless-stopped

# ============================================================================
# Application Ports
# ============================================================================
TRIGR_APP_PORT=6650
TRIGR_APP_METRICS_PORT=6740
TRIGR_APP_WORKER_PORT=6651
TRIGR_APP_ENGINES_PORT=6652

# ============================================================================
# Application Environment
# ============================================================================
TRIGR_APP_NODE_ENV=production
TRIGR_LOG_LEVEL=info
TRIGR_OTEL_SERVICE_NAME=triggerra.app

# ============================================================================
# OpenBao Agent Configuration
# ============================================================================
TRIGR_BAO_AGENT_CONFIG=/openbao/agent-config.hcl
TRIGR_APPROLE_ROLE_ID_PATH=../../.secrets/approle/triggerra/role-id
TRIGR_APPROLE_SECRET_ID_PATH=../../.secrets/approle/triggerra/secret-id

# ============================================================================
# Traefik Configuration
# ============================================================================
TRIGR_TRAEFIK_ENABLE=true
TRIGR_TRAEFIK_HOST=triggerra.${SUITE_APP_DOMAIN:-geniuserp.app}
TRIGR_TRAEFIK_ENTRYPOINT=websecure
TRIGR_TRAEFIK_TLS=true
TRIGR_TRAEFIK_CERTRESOLVER=letsencrypt
TRIGR_TRAEFIK_MIDDLEWARES=global-chain@file

# ============================================================================
# Health Check Configuration
# ============================================================================
TRIGR_HEALTHCHECK_INTERVAL=30s
TRIGR_HEALTHCHECK_TIMEOUT=10s
TRIGR_HEALTHCHECK_RETRIES=3
TRIGR_HEALTHCHECK_START_PERIOD=60s

# ============================================================================
# SECRETS - DO NOT DEFINE HERE!
# ============================================================================
# The following secrets are injected via OpenBao Agent:
# - TRIGR_DB_USER (dynamic from database/creds/triggerra_runtime)
# - TRIGR_DB_PASS (dynamic from database/creds/triggerra_runtime)
# - TRIGR_JWT_SECRET (static from kv/data/apps/triggerra)
# - TRIGR_API_KEY (static from kv/data/apps/triggerra)
# - TRIGR_ENCRYPTION_KEY (static from kv/data/apps/triggerra)
#
# These are rendered by OpenBao Agent into /app/secrets/.env
# DO NOT add them to this file!
