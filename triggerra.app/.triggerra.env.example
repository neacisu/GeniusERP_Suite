# Triggerra Application Configuration
# Copy this file to .triggerra.env and fill in the values
# NOTE: This file contains NON-SECRET configuration only
# SECRETS (DB passwords, JWT secrets, API keys) are injected via OpenBao Agent

# ============================================================================
# Container Configuration
# ============================================================================
TRIG_CONTAINER_NAME=genius-suite-triggerra-app
TRIG_RESTART_POLICY=unless-stopped

# ============================================================================
# Application Ports
# ============================================================================
TRIG_APP_PORT=6650
TRIG_APP_METRICS_PORT=6740
TRIG_APP_WORKER_PORT=6651

# ============================================================================
# Application Environment
# ============================================================================
TRIG_APP_NODE_ENV=production
TRIG_LOG_LEVEL=info
TRIG_OTEL_SERVICE_NAME=triggerra.app

# ============================================================================
# OpenBao Agent Configuration
# ============================================================================
TRIG_BAO_AGENT_CONFIG=/openbao/agent-config.hcl
TRIG_APPROLE_ROLE_ID_PATH=../../.secrets/approle/triggerra/role-id
TRIG_APPROLE_SECRET_ID_PATH=../../.secrets/approle/triggerra/secret-id

# ============================================================================
# Traefik Configuration
# ============================================================================
TRIG_TRAEFIK_ENABLE=true
TRIG_TRAEFIK_HOST=triggerra.${SUITE_APP_DOMAIN:-geniuserp.app}
TRIG_TRAEFIK_ENTRYPOINT=websecure
TRIG_TRAEFIK_TLS=true
TRIG_TRAEFIK_CERTRESOLVER=letsencrypt
TRIG_TRAEFIK_MIDDLEWARES=global-chain@file

# ============================================================================
# Health Check Configuration
# ============================================================================
TRIG_HEALTHCHECK_INTERVAL=30s
TRIG_HEALTHCHECK_TIMEOUT=10s
TRIG_HEALTHCHECK_RETRIES=3
TRIG_HEALTHCHECK_START_PERIOD=60s

# ============================================================================
# SECRETS - DO NOT DEFINE HERE!
# ============================================================================
# The following secrets are injected via OpenBao Agent:
# - TRIG_DB_USER (dynamic from database/creds/triggerra_runtime)
# - TRIG_DB_PASS (dynamic from database/creds/triggerra_runtime)
# - TRIG_JWT_SECRET (static from kv/data/apps/triggerra)
# - TRIG_API_KEY (static from kv/data/apps/triggerra)
# - TRIG_ENCRYPTION_KEY (static from kv/data/apps/triggerra)
#
# These are rendered by OpenBao Agent into /app/secrets/.env
# DO NOT add them to this file!
