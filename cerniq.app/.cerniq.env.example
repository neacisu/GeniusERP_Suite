# Cerniq Application Configuration
# Copy this file to .cerniq.env and fill in the values
# NOTE: This file contains NON-SECRET configuration only
# SECRETS (DB passwords, JWT secrets, API keys) are injected via OpenBao Agent

# ============================================================================
# Container Configuration
# ============================================================================
CERN_CONTAINER_NAME=genius-suite-cerniq-app
CERN_RESTART_POLICY=unless-stopped

# ============================================================================
# Application Ports
# ============================================================================
CERN_APP_PORT=6800
CERN_APP_METRICS_PORT=6890
CERN_APP_WORKER_PORT=6801

# ============================================================================
# Application Environment
# ============================================================================
CERN_APP_NODE_ENV=production
CERN_LOG_LEVEL=info
CERN_OTEL_SERVICE_NAME=cerniq.app

# ============================================================================
# OpenBao Agent Configuration
# ============================================================================
CERN_BAO_AGENT_CONFIG=/openbao/agent-config.hcl
CERN_APPROLE_ROLE_ID_PATH=../../.secrets/approle/cerniq/role-id
CERN_APPROLE_SECRET_ID_PATH=../../.secrets/approle/cerniq/secret-id

# ============================================================================
# Traefik Configuration
# ============================================================================
CERN_TRAEFIK_ENABLE=true
CERN_TRAEFIK_HOST=cerniq.${SUITE_APP_DOMAIN:-geniuserp.app}
CERN_TRAEFIK_ENTRYPOINT=websecure
CERN_TRAEFIK_TLS=true
CERN_TRAEFIK_CERTRESOLVER=letsencrypt
CERN_TRAEFIK_MIDDLEWARES=global-chain@file

# ============================================================================
# Health Check Configuration
# ============================================================================
CERN_HEALTHCHECK_INTERVAL=30s
CERN_HEALTHCHECK_TIMEOUT=10s
CERN_HEALTHCHECK_RETRIES=3
CERN_HEALTHCHECK_START_PERIOD=60s

# ============================================================================
# SECRETS - DO NOT DEFINE HERE!
# ============================================================================
# The following secrets are injected via OpenBao Agent:
# - CERN_DB_USER (dynamic from database/creds/cerniq_runtime)
# - CERN_DB_PASS (dynamic from database/creds/cerniq_runtime)
# - CERN_JWT_SECRET (static from kv/data/apps/cerniq)
# - CERN_API_KEY (static from kv/data/apps/cerniq)
# - CERN_ENCRYPTION_KEY (static from kv/data/apps/cerniq)
#
# These are rendered by OpenBao Agent into /app/secrets/.env
# DO NOT add them to this file!
