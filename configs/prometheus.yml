global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  # - "first_rules.yml"
  # - "second_rules.yml"

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  - job_name: 'otel-collector'
    static_configs:
      - targets: ['otel-collector:55679']

  # Add more jobs for app services here
  # - job_name: 'genius-suite-api'
  #   static_configs:
  #     - targets: ['api:3000']  # assuming /metrics on port 3000