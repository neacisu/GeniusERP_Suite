# Flowxify Application Configuration
# Copy this file to .flowxify.env and fill in the values
# NOTE: This file contains NON-SECRET configuration only
# SECRETS (DB passwords, JWT secrets, API keys) are injected via OpenBao Agent

# ============================================================================
# Container Configuration
# ============================================================================
FLOWX_CONTAINER_NAME=genius-suite-flowxify-app
FLOWX_RESTART_POLICY=unless-stopped

# ============================================================================
# Application Ports
# ============================================================================
FLOWX_APP_PORT=6600
FLOWX_APP_METRICS_PORT=6690
FLOWX_APP_WORKER_PORT=6601
FLOWX_APP_REALTIME_GATEWAY_PORT=6602

# ============================================================================
# Application Environment
# ============================================================================
FLOWX_APP_NODE_ENV=production
FLOWX_LOG_LEVEL=info
FLOWX_OTEL_SERVICE_NAME=flowxify.app

# ============================================================================
# OpenBao Agent Configuration
# ============================================================================
FLOWX_BAO_AGENT_CONFIG=/openbao/agent-config.hcl
FLOWX_APPROLE_ROLE_ID_PATH=../../.secrets/approle/flowxify/role-id
FLOWX_APPROLE_SECRET_ID_PATH=../../.secrets/approle/flowxify/secret-id

# ============================================================================
# Traefik Configuration
# ============================================================================
FLOWX_TRAEFIK_ENABLE=true
FLOWX_TRAEFIK_HOST=flowxify.${SUITE_APP_DOMAIN:-geniuserp.app}
FLOWX_TRAEFIK_ENTRYPOINT=websecure
FLOWX_TRAEFIK_TLS=true
FLOWX_TRAEFIK_CERTRESOLVER=letsencrypt
FLOWX_TRAEFIK_MIDDLEWARES=global-chain@file

# ============================================================================
# Health Check Configuration
# ============================================================================
FLOWX_HEALTHCHECK_INTERVAL=30s
FLOWX_HEALTHCHECK_TIMEOUT=10s
FLOWX_HEALTHCHECK_RETRIES=3
FLOWX_HEALTHCHECK_START_PERIOD=60s

# ============================================================================
# SECRETS - DO NOT DEFINE HERE!
# ============================================================================
# The following secrets are injected via OpenBao Agent:
# - FLOWX_DB_USER (dynamic from database/creds/flowxify_runtime)
# - FLOWX_DB_PASS (dynamic from database/creds/flowxify_runtime)
# - FLOWX_JWT_SECRET (static from kv/data/apps/flowxify)
# - FLOWX_API_KEY (static from kv/data/apps/flowxify)
# - FLOWX_ENCRYPTION_KEY (static from kv/data/apps/flowxify)
#
# These are rendered by OpenBao Agent into /app/secrets/.env
# DO NOT add them to this file!
