#######################################################
# CONTROL PLANE: SUITE-SHELL (CP_SHELL)
# Sursă: Tabelul 5 - Interval 6100-6149
#######################################################

# -- CONFIGURARE APLICAȚIE --
# Portul intern al API-ului Suite Shell
CP_SHELL_APP_PORT=6100
CP_SHELL_APP_METRICS_PORT=6101
CP_SHELL_APP_NODE_ENV=production

# -- AUTENTIFICARE --
CP_SHELL_AUTH_IDENTITY_URL=http://identity:6250

# -- CONFIGURĂRI SHELL --
# Timeout pentru execuția comenzilor (în secunde)
CP_SHELL_EXEC_TIMEOUT_SECONDS=30

# Director de lucru implicit
CP_SHELL_WORKING_DIR=/tmp

# -- API PROVIDERS --
# Integrări pentru shell operations
CP_SHELL_API_SANDBOX_ENABLED=true

# -- OBSERVABILITATE --
CP_SHELL_OBS_OTEL_ENDPOINT=${SUITE_OBS_OTEL_COLLECTOR_GRPC_URL}
CP_SHELL_OBS_SERVICE_NAME=suite-shell
CP_SHELL_OBS_METRICS_PORT=6101

# -- PROCESS SUPERVISOR / OPENBAO --
CP_SHELL_BAO_AGENT_CONFIG=/openbao/agent-config.hcl
CP_SHELL_APPROLE_ROLE_ID_PATH=../../../.secrets/approle/cp-suite-shell/role-id
CP_SHELL_APPROLE_SECRET_ID_PATH=../../../.secrets/approle/cp-suite-shell/secret-id

# -- SECRETE (INJECTATE VIA OPENBAO) --
# Nu defini aici următoarele variabile sensibile. Sunt randate de OpenBao în /app/secrets/.env:
# - CP_SHELL_DB_POSTGRES_URL (din database/creds/cp_suite_shell_runtime)
# - CP_SHELL_AUTH_JWT_SECRET (din kv/data/cp/suite-shell)