# Suite-Shell Application Configuration
# Copy this file to .suite-shell.env and fill in the values
# NOTE: This file contains NON-SECRET configuration only
# SECRETS (DB passwords, JWT secrets, API keys) are injected via OpenBao Agent

# ============================================================================
# Container Configuration
# ============================================================================
SHELL_CONTAINER_NAME=genius-suite-suite-shell-app
SHELL_RESTART_POLICY=unless-stopped

# ============================================================================
# Application Ports
# ============================================================================
SHELL_APP_PORT=6400
SHELL_APP_METRICS_PORT=6490
SHELL_APP_WORKER_PORT=6401

# ============================================================================
# Application Environment
# ============================================================================
SHELL_APP_NODE_ENV=production
SHELL_LOG_LEVEL=info
SHELL_OTEL_SERVICE_NAME=suite-shell.app

# ============================================================================
# OpenBao Agent Configuration
# ============================================================================
SHELL_BAO_AGENT_CONFIG=/openbao/agent-config.hcl
SHELL_APPROLE_ROLE_ID_PATH=../../.secrets/approle/suite-shell/role-id
SHELL_APPROLE_SECRET_ID_PATH=../../.secrets/approle/suite-shell/secret-id

# ============================================================================
# Traefik Configuration
# ============================================================================
SHELL_TRAEFIK_ENABLE=true
SHELL_TRAEFIK_HOST=suite-shell.${SUITE_APP_DOMAIN:-geniuserp.app}
SHELL_TRAEFIK_ENTRYPOINT=websecure
SHELL_TRAEFIK_TLS=true
SHELL_TRAEFIK_CERTRESOLVER=letsencrypt
SHELL_TRAEFIK_MIDDLEWARES=global-chain@file

# ============================================================================
# Health Check Configuration
# ============================================================================
SHELL_HEALTHCHECK_INTERVAL=30s
SHELL_HEALTHCHECK_TIMEOUT=10s
SHELL_HEALTHCHECK_RETRIES=3
SHELL_HEALTHCHECK_START_PERIOD=60s

# ============================================================================
# SECRETS - DO NOT DEFINE HERE!
# ============================================================================
# The following secrets are injected via OpenBao Agent:
# - SHELL_DB_USER (dynamic from database/creds/suite-shell_runtime)
# - SHELL_DB_PASS (dynamic from database/creds/suite-shell_runtime)
# - SHELL_JWT_SECRET (static from kv/data/apps/suite-shell)
# - SHELL_API_KEY (static from kv/data/apps/suite-shell)
# - SHELL_ENCRYPTION_KEY (static from kv/data/apps/suite-shell)
#
# These are rendered by OpenBao Agent into /app/secrets/.env
# DO NOT add them to this file!
