# Identity Application Configuration
# Copy this file to .identity.env and fill in the values
# NOTE: This file contains NON-SECRET configuration only
# SECRETS (DB passwords, JWT secrets, API keys) are injected via OpenBao Agent

# ============================================================================
# Container Configuration
# ============================================================================
IDENT_CONTAINER_NAME=genius-suite-identity-app
IDENT_RESTART_POLICY=unless-stopped

# ============================================================================
# Application Ports
# ============================================================================
IDENT_APP_PORT=6250
IDENT_APP_METRICS_PORT=6340
IDENT_APP_WORKER_PORT=6251

# ============================================================================
# Application Environment
# ============================================================================
IDENT_APP_NODE_ENV=production
IDENT_LOG_LEVEL=info
IDENT_OTEL_SERVICE_NAME=identity.app

# ============================================================================
# OpenBao Agent Configuration
# ============================================================================
IDENT_BAO_AGENT_CONFIG=/openbao/agent-config.hcl
IDENT_APPROLE_ROLE_ID_PATH=../../.secrets/approle/identity/role-id
IDENT_APPROLE_SECRET_ID_PATH=../../.secrets/approle/identity/secret-id

# ============================================================================
# Traefik Configuration
# ============================================================================
IDENT_TRAEFIK_ENABLE=true
IDENT_TRAEFIK_HOST=identity.${SUITE_APP_DOMAIN:-geniuserp.app}
IDENT_TRAEFIK_ENTRYPOINT=websecure
IDENT_TRAEFIK_TLS=true
IDENT_TRAEFIK_CERTRESOLVER=letsencrypt
IDENT_TRAEFIK_MIDDLEWARES=global-chain@file

# ============================================================================
# Health Check Configuration
# ============================================================================
IDENT_HEALTHCHECK_INTERVAL=30s
IDENT_HEALTHCHECK_TIMEOUT=10s
IDENT_HEALTHCHECK_RETRIES=3
IDENT_HEALTHCHECK_START_PERIOD=60s

# ============================================================================
# SECRETS - DO NOT DEFINE HERE!
# ============================================================================
# The following secrets are injected via OpenBao Agent:
# - IDENT_DB_USER (dynamic from database/creds/identity_runtime)
# - IDENT_DB_PASS (dynamic from database/creds/identity_runtime)
# - IDENT_JWT_SECRET (static from kv/data/apps/identity)
# - IDENT_API_KEY (static from kv/data/apps/identity)
# - IDENT_ENCRYPTION_KEY (static from kv/data/apps/identity)
#
# These are rendered by OpenBao Agent into /app/secrets/.env
# DO NOT add them to this file!
