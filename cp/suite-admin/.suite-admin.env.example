# Suite-Admin Application Configuration
# Copy this file to .suite-admin.env and fill in the values
# NOTE: This file contains NON-SECRET configuration only
# SECRETS (DB passwords, JWT secrets, API keys) are injected via OpenBao Agent

# ============================================================================
# Container Configuration
# ============================================================================
SADMIN_CONTAINER_NAME=genius-suite-suite-admin-app
SADMIN_RESTART_POLICY=unless-stopped

# ============================================================================
# Application Ports
# ============================================================================
SADMIN_APP_PORT=6350
SADMIN_APP_METRICS_PORT=6440
SADMIN_APP_WORKER_PORT=6351

# ============================================================================
# Application Environment
# ============================================================================
SADMIN_APP_NODE_ENV=production
SADMIN_LOG_LEVEL=info
SADMIN_OTEL_SERVICE_NAME=suite-admin.app

# ============================================================================
# OpenBao Agent Configuration
# ============================================================================
SADMIN_BAO_AGENT_CONFIG=/openbao/agent-config.hcl
SADMIN_APPROLE_ROLE_ID_PATH=../../.secrets/approle/suite-admin/role-id
SADMIN_APPROLE_SECRET_ID_PATH=../../.secrets/approle/suite-admin/secret-id

# ============================================================================
# Traefik Configuration
# ============================================================================
SADMIN_TRAEFIK_ENABLE=true
SADMIN_TRAEFIK_HOST=suite-admin.${SUITE_APP_DOMAIN:-geniuserp.app}
SADMIN_TRAEFIK_ENTRYPOINT=websecure
SADMIN_TRAEFIK_TLS=true
SADMIN_TRAEFIK_CERTRESOLVER=letsencrypt
SADMIN_TRAEFIK_MIDDLEWARES=global-chain@file

# ============================================================================
# Health Check Configuration
# ============================================================================
SADMIN_HEALTHCHECK_INTERVAL=30s
SADMIN_HEALTHCHECK_TIMEOUT=10s
SADMIN_HEALTHCHECK_RETRIES=3
SADMIN_HEALTHCHECK_START_PERIOD=60s

# ============================================================================
# SECRETS - DO NOT DEFINE HERE!
# ============================================================================
# The following secrets are injected via OpenBao Agent:
# - SADMIN_DB_USER (dynamic from database/creds/suite-admin_runtime)
# - SADMIN_DB_PASS (dynamic from database/creds/suite-admin_runtime)
# - SADMIN_JWT_SECRET (static from kv/data/apps/suite-admin)
# - SADMIN_API_KEY (static from kv/data/apps/suite-admin)
# - SADMIN_ENCRYPTION_KEY (static from kv/data/apps/suite-admin)
#
# These are rendered by OpenBao Agent into /app/secrets/.env
# DO NOT add them to this file!
