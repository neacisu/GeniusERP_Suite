# Licensing Application Configuration
# Copy this file to .licensing.env and fill in the values
# NOTE: This file contains NON-SECRET configuration only
# SECRETS (DB passwords, JWT secrets, API keys) are injected via OpenBao Agent

# ============================================================================
# Container Configuration
# ============================================================================
LIC_CONTAINER_NAME=genius-suite-licensing-app
LIC_RESTART_POLICY=unless-stopped

# ============================================================================
# Application Ports
# ============================================================================
LIC_APP_PORT=6300
LIC_APP_METRICS_PORT=6390
LIC_APP_WORKER_PORT=6301

# ============================================================================
# Application Environment
# ============================================================================
LIC_APP_NODE_ENV=production
LIC_LOG_LEVEL=info
LIC_OTEL_SERVICE_NAME=licensing.app

# ============================================================================
# OpenBao Agent Configuration
# ============================================================================
LIC_BAO_AGENT_CONFIG=/openbao/agent-config.hcl
LIC_APPROLE_ROLE_ID_PATH=../../.secrets/approle/licensing/role-id
LIC_APPROLE_SECRET_ID_PATH=../../.secrets/approle/licensing/secret-id

# ============================================================================
# Traefik Configuration
# ============================================================================
LIC_TRAEFIK_ENABLE=true
LIC_TRAEFIK_HOST=licensing.${SUITE_APP_DOMAIN:-geniuserp.app}
LIC_TRAEFIK_ENTRYPOINT=websecure
LIC_TRAEFIK_TLS=true
LIC_TRAEFIK_CERTRESOLVER=letsencrypt
LIC_TRAEFIK_MIDDLEWARES=global-chain@file

# ============================================================================
# Health Check Configuration
# ============================================================================
LIC_HEALTHCHECK_INTERVAL=30s
LIC_HEALTHCHECK_TIMEOUT=10s
LIC_HEALTHCHECK_RETRIES=3
LIC_HEALTHCHECK_START_PERIOD=60s

# ============================================================================
# SECRETS - DO NOT DEFINE HERE!
# ============================================================================
# The following secrets are injected via OpenBao Agent:
# - LIC_DB_USER (dynamic from database/creds/licensing_runtime)
# - LIC_DB_PASS (dynamic from database/creds/licensing_runtime)
# - LIC_JWT_SECRET (static from kv/data/apps/licensing)
# - LIC_API_KEY (static from kv/data/apps/licensing)
# - LIC_ENCRYPTION_KEY (static from kv/data/apps/licensing)
#
# These are rendered by OpenBao Agent into /app/secrets/.env
# DO NOT add them to this file!
