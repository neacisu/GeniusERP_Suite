# Mercantiq Application Configuration
# Copy this file to .mercantiq.env and fill in the values
# NOTE: This file contains NON-SECRET configuration only
# SECRETS (DB passwords, JWT secrets, API keys) are injected via OpenBao Agent

# ============================================================================
# Container Configuration
# ============================================================================
MERCQ_CONTAINER_NAME=genius-suite-mercantiq-app
MERCQ_RESTART_POLICY=unless-stopped

# ============================================================================
# Application Ports
# ============================================================================
MERCQ_APP_PORT=6700
MERCQ_APP_METRICS_PORT=9092
MERCQ_APP_ENGINES_PORT=6701

# ============================================================================
# Application Environment
# ============================================================================
MERCQ_APP_NODE_ENV=production
MERCQ_LOG_LEVEL=info
MERCQ_OTEL_SERVICE_NAME=mercantiq.app

# ============================================================================
# OpenBao Agent Configuration
# ============================================================================
MERCQ_BAO_AGENT_CONFIG=/openbao/agent-config.hcl
MERCQ_APPROLE_ROLE_ID_PATH=../../.secrets/approle/mercantiq/role-id
MERCQ_APPROLE_SECRET_ID_PATH=../../.secrets/approle/mercantiq/secret-id

# ============================================================================
# Traefik Configuration
# ============================================================================
MERCQ_TRAEFIK_ENABLE=true
MERCQ_TRAEFIK_HOST=mercantiq.${SUITE_APP_DOMAIN:-geniuserp.app}
MERCQ_TRAEFIK_ENTRYPOINT=websecure
MERCQ_TRAEFIK_TLS=true
MERCQ_TRAEFIK_CERTRESOLVER=letsencrypt
MERCQ_TRAEFIK_MIDDLEWARES=global-chain@file

# ============================================================================
# Health Check Configuration
# ============================================================================
MERCQ_HEALTHCHECK_INTERVAL=30s
MERCQ_HEALTHCHECK_TIMEOUT=10s
MERCQ_HEALTHCHECK_RETRIES=3
MERCQ_HEALTHCHECK_START_PERIOD=60s

# ============================================================================
# SECRETS - DO NOT DEFINE HERE!
# ============================================================================
# The following secrets are injected via OpenBao Agent:
# - MERCQ_DB_USER (dynamic from database/creds/mercantiq_runtime)
# - MERCQ_DB_PASS (dynamic from database/creds/mercantiq_runtime)
# - MERCQ_JWT_SECRET (static from kv/data/apps/mercantiq)
# - MERCQ_API_KEY (static from kv/data/apps/mercantiq)
# - MERCQ_ENCRYPTION_KEY (static from kv/data/apps/mercantiq)
#
# These are rendered by OpenBao Agent into /app/secrets/.env
# DO NOT add them to this file!
